server:
  port: 10001

spring:
  application:
    name: Piggy-Common

  data:
    redis:
      host: localhost
      port: 6379   # Redis 默认端口号
      password: ${redis.password:20050603RJ}
      database: 5
      timeout: 10000ms



jwt:
  secret: "PiggyMiniSecretKeyForJWTSigning2026SecureString"
  expiration: 3600000
